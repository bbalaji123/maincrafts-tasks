# Frontend

Open `frontend/index.html` in a browser. The page loads React and Babel via CDN. The app will fetch data from the backend at `http://localhost:4000/api` by default.

For development, start backend and then open the HTML file in your browser (or serve via static server).
